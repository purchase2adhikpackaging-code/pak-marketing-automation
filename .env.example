NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Publishing worker privileged operations are brokered through Supabase Edge/Vault.
# Do not add SUPABASE_SERVICE_ROLE_KEY to the Vercel publishing runtime.
# Optional until the LTX worker endpoint is enabled.
LTX_WORKER_SHARED_SECRET=
AI_TEXT_PROVIDER=fake
