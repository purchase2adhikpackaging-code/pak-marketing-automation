NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Server-only. Required by the approved Vercel Node publishing worker runtime for
# privileged production queue RPCs, private publishing-books Storage access, and
# publication registration. Never expose this value to browser/client code.
SUPABASE_SERVICE_ROLE_KEY=
# Optional until the LTX worker endpoint is enabled.
LTX_WORKER_SHARED_SECRET=
AI_TEXT_PROVIDER=fake
