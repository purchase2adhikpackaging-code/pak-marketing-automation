import { ModulePage } from "@/components/app-shell/module-page";

export default function PublishingPage() {
  return <ModulePage title="Publishing" description="Manage approved publication schedules, platform adapters, attempts, external identifiers, and publish outcomes." />;
}
