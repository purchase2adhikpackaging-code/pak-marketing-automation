import { ModulePage } from "@/components/app-shell/module-page";

export default function SettingsPage() {
  return <ModulePage title="Settings" description="Manage organization-scoped platform configuration, access controls, providers, and operational preferences." />;
}
